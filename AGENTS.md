<!--
  Agent-Instruktionen für dieses Repo (RESEARCH). Ergänzt README.md.
-->

# AGENTS.md

## Agent skills

### Issue tracker

Issues leben als GitHub Issues in `PatrickFrantzen/research`, verwaltet über die `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Standard-Fünf-Rollen-Vokabular (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), 1:1 übernommen. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context Layout: `CONTEXT.md` + `docs/adr/` am Repo-Root (beide noch nicht angelegt, entstehen bei Bedarf über `/domain-modeling`). See `docs/agents/domain.md`.
